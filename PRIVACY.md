# ChatStack 隐私政策

**最后更新：2026-08-25**

## 1. 适用范围

ChatStack 是一个在 ChatGPT、Gemini、Claude 和 DeepSeek 对话页面运行的本地优先浏览器扩展。它提供目录、搜索、历史整理、项目分组、收藏和导出功能。

## 2. 扩展处理的数据

为了提供上述功能，扩展会在浏览器中读取当前对话页面的内容和必要的页面标识。用户启用自动保存时，扩展可能在 `chrome.storage.local` 中保存：

- 对话标题、对话标识和消息内容；
- 本地项目分组、收藏和筛选状态；
- 语言、主题、侧边栏宽度等设置。

这些数据仅在用户自己的浏览器配置中使用。

## 3. 不进行的处理

ChatStack：

- 没有开发者运营的后端或账户系统；
- 不包含遥测、广告或分析 SDK；
- 不会把对话、项目、收藏或设置发送给开发者；
- 不会把上述数据出售或分享给第三方；
- 不会为显示平台图标额外请求第三方图标服务。

扩展读取内容所在的 AI 平台仍会按照其自身政策处理用户在该平台上的活动，这不属于 ChatStack 的数据处理。

## 4. 权限用途

- `storage`：在浏览器本地保存扩展数据和设置。
- `chat.openai.com`、`chatgpt.com`、`gemini.google.com`、`claude.ai`、`chat.deepseek.com` 的主机权限：只在这些页面注入侧边栏、读取对话 DOM，并响应用户主动执行的跳转或导出操作。

ChatStack 不申请浏览历史、Cookie、下载记录或所有网站访问权限。

## 5. 用户控制与删除

- 用户可关闭自动保存，阻止新解析内容写入扩展历史。
- 用户可在扩展设置中执行“清空所有数据”。
- 用户也可以通过浏览器的扩展管理页面清除扩展数据或卸载 ChatStack。
- 导出的文件由用户主动生成并保存在用户选择的本地位置，后续由用户自行管理。

## 6. 安全与更新

生产版本默认关闭包含会话或项目数据的调试日志。若本政策或产品的数据处理方式发生变化，本文件会同步更新日期和说明。

问题与安全报告方式见 [SECURITY.md](SECURITY.md)。
